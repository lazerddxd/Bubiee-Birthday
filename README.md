# Bubiee Birthday Studio
Full-stack project: HTML5, CSS3, JavaScript, Node.js, JSON API, Canvas, WAV, plus optional ASP.NET Core/.NET 8 project.

Node.js: install Node 18+, run `npm start`, then open http://localhost:3000.
Visual Studio: open `BubieeBirthday.sln` to inspect/build the .NET project.
